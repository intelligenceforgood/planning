# 2025-12-08 Status Update

## Highlights
- Hybrid ingest/search hardened: dual-write retry drain validated; schema/entity expansion (browser/IP/ASN) live; saved-search payloads normalized across API/Streamlit/Next.js.
- Saved search UX: Next.js `/search` smoke covers taxonomy/entity filters and saved-search reruns; SDK/REST carry schema_version + tags so history badges render correctly.
- Evidence dossier prototype: portal parity (download + Web Crypto verify), signature manifests, Drive upload path, and nightly smoke harnesses landed; pilot assets and regression harness refreshed.
- IAP + domains working: Cloud Run console/API now fronted by HTTPS LB + IAP with custom domains wired; Terraform apply succeeded after command override removal and port alignment.

## In Flight
- PII vault spike (separate from M4): scope KMS/Secret Manager, cross-project access, and Terraform layout for `pii-vault/{dev,prod}`.
- Keep hybrid search + dossier smokes running to catch regressions.

## Risks
- Nightly dossier/hybrid smokes are the main guardrail; skips could hide regressions.

## Next Steps
- Kick off PII vault design/PoC: projects, KMS keys, secrets, and app SA access pattern.
- Continue nightly smokes (hybrid search, dossier) for early warning.

## Requests
- Confirm vault scope/naming before Terraform scaffold (projects, key names, secret namespaces).
