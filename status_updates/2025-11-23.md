# 2025-11-23 Status Update

## Highlights
- Locked in the identity pivot after repeated GIS helper failures: removed the Quick Auth SPA, purged the associated Terraform inputs, and updated `docs/iam.md` so all Cloud Run surfaces now assume IAP + analyst-group enforcement (per 2025-11-21 change-log entry).
- Consolidated FastAPI, Streamlit, and the Next.js console onto the shared `sa-app` runtime account and tightened Cloud Run IAM bindings; Terraform now manages the `roles/run.invoker` policy so only the runtime account plus `i4g_analyst_members` reach the services (2025-11-20 log).
- Restored analyst-console intake UX: fixed the missing `I4G_API_KEY` in dev Terraform, added spinner states + clearer copy to the intake form, and kept the Discovery Engine redaction / search-loading improvements so demos show progress and privacy cues end-to-end.

## In Flight
- Authoring Terraform for the new IAP policy bindings (analyst Google Groups + service identities) so the identity pivot is enforced in both dev and prod once applies run.
- Monitoring Cloud Run job smoke tests after the ingestion/report env-var refresh; still need to automate the intake → job check in CI or a scheduled Cloud Build task.
- Continuing iteration on analyst-console ergonomics (Discovery defaults, redaction breadth, spinner visibility) as stakeholder feedback lands.

## Risks
- IAP enforcement is partially manual until Terraform applies the new bindings; any drift between console/FastAPI services could reopen unauthenticated paths.
- Smoke coverage for Cloud Run jobs is manual; without automation regressions could linger until the next operator-driven test.
- Dev FastAPI tokens remain static (`dev-analyst-token`); if rotated without doc updates the intake workflow will fail silently for analysts.

## Next Steps
- Land Terraform changes for the IAP policy + analyst groups, then apply to `i4g-dev` followed by `i4g-prod` once verified.
- Script the dev smoke workflow (submit intake via API, trigger Cloud Run job, verify attachments) and add it to the weekly refresh cadence.
- Expand analyst-console redaction (phone/SSN patterns) and finalize the Discovery defaults logic ahead of the next stakeholder review.

## Requests
- Need confirmation of the final analyst Google Group(s) that should receive IAP access before promoting the Terraform change.
- Heads-up if the dev FastAPI token is rotated so docs and Terraform vars stay in sync.
